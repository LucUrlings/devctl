#!/usr/bin/env bash
set -euo pipefail

case ${1:-herdr} in
  herdr)
    docker version >/dev/null
    herdr status server >/dev/null
    [[ -S ${HERDR_SOCKET_PATH:?} ]]
    ;;
  ccgram)
    [[ -S ${HERDR_SOCKET_PATH:?} ]]
    pgrep -f '(^|/)ccgram($| )' >/dev/null
    ;;
  *) exit 2 ;;
esac
